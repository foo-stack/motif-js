---
title: Code
description: Inline `<code>` - monospace font, subtle background tint. For code spans inside running text.
diataxis: reference
covers: ['Code', 'CodeProps']
last_verified: 2026-05-15
---

import {
  ApiSignature,
  ArticleMeta,
  Callout,
  CodeBlock,
  ComponentDemoStrip,
  Eyebrow,
  Lede,
} from '../../../components/index.js';

<Eyebrow>Components / Typography</Eyebrow>

# Code

<Lede>
  Code is an inline `<code>` span - monospace type and a subtle background tint. Reach for it when
  a token name, a prop, or a short literal appears inside running text.
</Lede>

<ComponentDemoStrip demo="code" />

<ArticleMeta />

## What it is

A `Text` rendered as `<code>` with a monospace font, a small font size, and a muted background
tint. Code is built for inline use - a span inside a paragraph. For multi-line code blocks, render
`Text` as `as="pre"` or use a dedicated syntax-highlighting component; Code does not preserve
whitespace or wrap.

## Install

`Code` is exported from `usemotif`. No separate install.

<CodeBlock lang="tsx" code={`import { Code } from 'usemotif';`} />

## API

<ApiSignature
  name="Code"
  status="stable"
  signature={`function Code(props: CodeProps): JSX.Element`}
  params={[
    {
      name: '...TextProps',
      type: 'Omit<TextProps, "as">',
      description: 'Every Text prop except `as` - Code always renders `<code>`. Override `bg` / `color` for accent variants.',
    },
  ]}
/>

## Examples

Inline code span:

<CodeBlock lang="tsx" code={`<Paragraph>
  Resolve a token with <Code>$colors.surface.base</Code>.
</Paragraph>`} />

Accent-tinted variant:

<CodeBlock lang="tsx" code={`<Code bg="$colors.status.errorSoft" color="$colors.status.error">
  deprecated
</Code>`} />

<Callout variant="info" title="Related">
  [Kbd](/components/typography/kbd) for keyboard-shortcut labels.
  [Text](/components/typography/text) is the atom Code composes from.
</Callout>
