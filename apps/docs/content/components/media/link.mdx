---
title: Link
description: An inline anchor primitive. Theme-aware colour, a visible focus ring, and a safe `rel` default for new-tab links.
diataxis: reference
covers: ['Link', 'LinkProps']
last_verified: 2026-05-15
---

import {
  ApiSignature,
  ArticleMeta,
  Callout,
  CodeBlock,
  ComponentDemoStrip,
  Eyebrow,
  Lede,
} from '../../../components/index.js';

<Eyebrow>Components / Media</Eyebrow>

# Link

<Lede>
  Link is the inline anchor. It renders an `<a>`, takes its colour from the theme's accent, ships a
  visible focus ring, and auto-injects a safe `rel` when the link opens in a new tab.
</Lede>

<ComponentDemoStrip demo="link" />

<ArticleMeta />

## What it is

A `Pressable` rendered as `<a>`. Link inherits Pressable's pseudo-state plumbing — `_hover`,
`_focus`, and the rest all work — and layers anchor-specific behaviour on top: the `href`, the
`target`, an `underline` mode, and the `rel` safety default.

When `target="_blank"` is set, Link defaults `rel` to `noopener noreferrer`. The new tab cannot
reach back into the opener's `window`, and the referrer is withheld. Pass an explicit `rel` to
override.

## Install

`Link` is exported from `usemotif`. No separate install.

<CodeBlock lang="tsx" code={`import { Link } from 'usemotif';`} />

## API

<ApiSignature
  name="Link"
  status="stable"
  signature={`function Link(props: LinkProps): JSX.Element`}
  params={[
    {
      name: 'href',
      type: 'string',
      required: true,
      description: 'The URL the link points at. Becomes the anchor `href`.',
    },
    {
      name: 'target',
      type: '"_blank" | "_self" | "_parent" | "_top"',
      description: 'Standard anchor target. `_blank` opens a new tab and triggers the safe `rel` default.',
    },
    {
      name: 'rel',
      type: 'string',
      description: 'Anchor `rel`. Defaults to `noopener noreferrer` when `target="_blank"`; pass a value to override.',
    },
    {
      name: 'underline',
      type: '"hover" | "always" | "never"',
      default: '"hover"',
      description: 'Underline mode. `hover` underlines on hover and focus; `always` underlines unconditionally; `never` never underlines — use sparingly.',
    },
    {
      name: '…PressableProps',
      type: 'Omit<PressableProps, "as" | "children">',
      description: 'Every Pressable prop except `as` and `children`. Pseudo-state props (`_hover`, `_focus`) and style props pass through.',
    },
  ]}
/>

## Accessibility

Link's defaults follow the accessible path. The colour comes from the theme accent, so links are
distinguishable from body text. The focus ring is visible — keyboard users can see where they are.
`underline="hover"` keeps an underline available on focus as well as hover.

`underline="never"` removes the only non-colour signal that the text is a link. Reach for it only
when another affordance makes the link unmistakable; on its own, colour is not enough for users
who cannot distinguish it.

For new-tab links, the `rel` safety default is automatic — but signal the new tab to the user too,
with link text or an adjacent icon, so the context switch is not a surprise.

## Examples

A basic inline link:

<CodeBlock lang="tsx" code={`<Paragraph>
  Read the <Link href="/docs">documentation</Link> to get started.
</Paragraph>`} />

A new-tab link — `rel` is set automatically:

<CodeBlock lang="tsx" code={`<Link href="https://example.com" target="_blank">
  External site
</Link>`} />

An always-underlined link with a hover colour:

<CodeBlock lang="tsx" code={`<Link
  href="/pricing"
  underline="always"
  _hover={{ color: '$colors.action.primary.bgHover' }}
>
  See pricing
</Link>`} />

<Callout variant="info" title="Related">
  [Pressable](/components/interactive/pressable) is the primitive Link composes from.
  [Button](/components/interactive/button) when the affordance triggers an action rather than
  navigation.
</Callout>
