---
title: VStack
description: Vertical stack - Stack with `direction="column"` baked in. Reads more clearly than `Stack` without a direction prop.
diataxis: reference
covers: ['VStack']
last_verified: 2026-05-15
---

import {
  ApiSignature,
  ArticleMeta,
  Callout,
  CodeBlock,
  ComponentDemoStrip,
  Eyebrow,
  Lede,
} from '../../../components/index.js';

<Eyebrow>Components / Layout</Eyebrow>

# VStack

<Lede>
  VStack is `<Stack direction="column">` named at the call site. Stack is column by default - VStack
  is mostly for readability.
</Lede>

<ComponentDemoStrip demo="vstack" />

<ArticleMeta />

## What it is

A sugar wrapper. `VStack` renders `<Stack direction="column" />` and accepts every other Stack
prop unchanged. Functionally identical to `<Stack>` with no `direction` prop - the name exists so
intent is obvious when a file mixes vertical and horizontal stacks.

## Install

`VStack` is exported from `usemotif`. No separate install.

<CodeBlock lang="tsx" code={`import { VStack } from 'usemotif';`} />

## API

<ApiSignature
  name="VStack"
  status="stable"
  signature={`function VStack(props: Omit<StackProps, \"direction\">): JSX.Element`}
  params={[
    {
      name: '...StackProps',
      type: 'Omit<StackProps, \"direction\">',
      description: 'Every Stack prop except `direction`. `gap`, `alignItems`, `justifyContent`, and all Box style props apply normally.',
    },
  ]}
/>

## Examples

Form field stack:

<CodeBlock lang="tsx" code={`<VStack gap="$3">
  <Field>
    <Label>Email</Label>
    <Input type="email" />
  </Field>
  <Field>
    <Label>Password</Label>
    <Input type="password" />
  </Field>
  <Button intent="primary">Sign in</Button>
</VStack>`} />

Mixed-direction layout - VStack and HStack side by side:

<CodeBlock lang="tsx" code={`<VStack gap="$4">
  <Heading>Settings</Heading>
  <HStack gap="$2">
    <Button intent="neutral">Cancel</Button>
    <Button intent="primary">Save</Button>
  </HStack>
</VStack>`} />

<Callout variant="info" title="Related">
  [HStack](/components/layout/hstack) is the row equivalent. [Stack](/components/layout/stack) is
  the parent primitive with a `direction` prop.
</Callout>
