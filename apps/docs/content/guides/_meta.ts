import type { MetaFile } from '@vorge/core';

const meta: MetaFile = {
  'design-system': 'Build a design system',
  'migrating-styled-components': 'Migrate from styled-components',
  performance: 'Performance',
  'server-rendering': 'Server-side rendering',
};

export default meta;
