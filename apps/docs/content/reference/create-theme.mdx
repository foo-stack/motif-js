---
title: createTheme
description: Construct a theme. A pass-through factory that narrows the tokens type so $-references are typed in callers.
diataxis: reference
covers: ['createTheme']
last_verified: 2026-05-15
---

import { ApiSignature, ArticleMeta, CodeBlock, Eyebrow } from '../../components/index.js';

<Eyebrow>API reference</Eyebrow>

<ApiSignature
  name="createTheme"
  status="stable"
  signature={`function createTheme<T extends TokenMap>(def: {
  readonly name: string;
  readonly tokens: T;
  readonly fonts?: readonly FontFace[];
  readonly root?: ThemeRootStyles;
  readonly reducedMotion?: ReducedMotionMode;
}): Theme & { readonly tokens: T }`}
  params={[
    {
      name: 'def.name',
      type: 'string',
      required: true,
      description:
        'Theme name. Used as the data-theme attribute value on web. Must be unique among themes registered with a single ThemeProvider.',
    },
    {
      name: 'def.tokens',
      type: 'TokenMap',
      required: true,
      description:
        'Full token tree (primitives interleaved with semantics). Top-level keys are scale names — colors, space, radii, fontSizes, etc. Leaves are literal values or $-prefixed token references.',
    },
    {
      name: 'def.fonts',
      type: 'readonly FontFace[]',
      description:
        'Web-only. @font-face declarations ThemeProvider emits into the document. No effect on native.',
    },
    {
      name: 'def.root',
      type: 'ThemeRootStyles',
      description:
        'Web-only. Base styles applied to the document root — body resets and the like — emitted by ThemeProvider.',
    },
    {
      name: 'def.reducedMotion',
      type: 'ReducedMotionMode',
      description:
        'Web-only. The prefers-reduced-motion policy for this theme. No effect on native.',
    },
  ]}
/>

<ArticleMeta />

## Description

A thin factory. The returned object is exactly the input - `createTheme` does no transformation
at runtime. The type parameter `T extends TokenMap` preserves the literal shape of
`Theme.tokens`, which is what lets `$`-reference paths be derived from it. Those paths
autocomplete once you declare the theme; see
[Token autocomplete](/guides/token-autocomplete).

Pass the result to `ThemeProvider`'s `themes` array. The provider emits the CSS variables and
sets `data-theme` to one of the theme names.

`fonts`, `root`, and `reducedMotion` are optional web-only emission hints — `<ThemeProvider>`
picks them up to emit `@font-face` rules, document-root styles, and the reduced-motion policy.
They have no effect on native, where there is no stylesheet to emit into.

## Examples

<CodeBlock lang="ts" code={`import { createTheme } from 'usemotif';

export const lightTheme = createTheme({
  name: 'light',
  tokens: {
    colors: {
      ink: '#1C1917',
      paper: '#FBF7F2',
      blue: { 500: '#2563EB', 600: '#1D4ED8' },
      surface: { base: '$colors.paper' },
      text: { default: '$colors.ink' },
      action: { primary: { bg: '$colors.blue.600', fg: '$colors.paper' } },
    },
    space: { 1: 4, 2: 8, 4: 16 },
    radii: { sm: 4, md: 8, lg: 12 },
  },
});`} />

Both primitive and semantic tokens live in the same tree. Semantic tokens reference primitives by
`$colors.<path>`; the resolver walks the dotted path until it reaches a literal value.

## Related

- [Tokens](/concepts/tokens) — the token model, primitive vs semantic layers.
- [Theming](/concepts/theming) — how a theme becomes active at runtime.
- [`ThemeProvider`](/reference/theme-provider) — `<ThemeProvider>` and `<Theme>` boundary.
