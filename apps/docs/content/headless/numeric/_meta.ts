import type { MetaFile } from '@vorge/core';

const meta: MetaFile = {
  index: 'Numeric',
  slider: 'Slider',
  'range-slider': 'RangeSlider',
  progress: 'Progress',
  'rating-input': 'RatingInput',
};

export default meta;
