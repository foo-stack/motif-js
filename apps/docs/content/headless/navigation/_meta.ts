import type { MetaFile } from '@vorge/core';

const meta: MetaFile = {
  index: 'Navigation',
  breadcrumb: 'Breadcrumb',
  pagination: 'Pagination',
  stepper: 'Stepper',
  toolbar: 'Toolbar',
};

export default meta;
