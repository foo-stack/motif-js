import type { MetaFile } from '@vorge/core';

const meta: MetaFile = {
  index: 'Date & time',
  calendar: 'Calendar',
  'date-picker': 'DatePicker',
  'time-input': 'TimeInput',
};

export default meta;
