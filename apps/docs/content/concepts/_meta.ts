import type { MetaFile } from '@vorge/core';

const meta: MetaFile = {
  tokens: 'Tokens',
  variants: 'Variants',
  theming: 'Theming',
  composition: 'Composition',
  responsive: 'Responsive',
};

export default meta;
