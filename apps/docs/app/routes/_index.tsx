import Introduction from '../pages/Introduction.mdx';

// Placeholder for Phase 0 — both `/` and `/docs/introduction` render
// the same content until the home page lands in Phase 3.
export default function Index() {
  return <Introduction />;
}
